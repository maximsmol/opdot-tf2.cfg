// some systems limit fps too much because of a recent bug
fps_max 0

// prevents a possible memory leak
// some source systems assume fps limit of 300
//fps_max 300

// do not limit fps when out of focus
// some systems and streaming can break focus detection
engine_no_focus_sleep 0
//engine_no_focus_sleep 50
