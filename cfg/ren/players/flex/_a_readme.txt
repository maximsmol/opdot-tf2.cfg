facial expressions
all flex features have some CPU impact

as far as I could tell from the source code:
  - r_flex disables rendering flexes
  - flex_rules disables converting flex definitions into weights

  so disabling either will effectively make no flexes happen
